## scratch

Temporary Python experiments and quick validation scripts live here.

Suggested usage:

- `scratch/test_*.py` for quick throwaway checks
- `scratch/sample.py` for runnable examples

Run files with `uv`:

```powershell
uv run python scratch/sample.py
```

Keep production code out of this folder.
